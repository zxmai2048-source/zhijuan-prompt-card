# Design Notes

Zhijuan Prompt Card uses a dark local-tool interface with restrained orange primary actions, compact result cards, and pill tags for fast scanning.

The floating panel keeps analysis, recreation prompt, copy actions, and generator handoff in one surface. Popup and options share the same visual system: near-black background, low-contrast borders, small status pills, and form controls that avoid browser defaults.
